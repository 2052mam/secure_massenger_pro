# گزارش وضعیت — دور سوم

هر هفت مورد برطرف شد. برای هر مورد ابتدا علت ریشه‌ای پیدا و اثبات شد، سپس اصلاح
انجام گرفت و در پایان با تست پوشش داده شد.

**۲۸۵ تست بک‌اند سبز است** (۲۶۴ تست قبلی + ۲۱ تست جدید این دور).

---

## ۱. عکس پروفایل در تنظیمات

صفحه تنظیمات تصویر را مستقیم می‌ساخت و از مسیر استاندارد آواتار استفاده نمی‌کرد.
اکنون از همان ویجت `ChatAvatar` استفاده می‌کند که در بقیه برنامه به کار می‌رود، پس
عکس کاربر در تنظیمات هم دیده می‌شود.

## ۲. پنل مدیریت — گزارش‌ها

**مشکل:** صف گزارش‌ها فقط خواندنی بود. نام کانال را نشان می‌داد اما راهی برای دیدن
محتوا، حذف کانال یا خارج‌کردن گزارش از حالت «در انتظار» وجود نداشت.

**اصلاح:** یک صفحه بررسی گزارش اضافه شد که:
- محتوای گروه/کانال گزارش‌شده را نشان می‌دهد (۵۰ پیام آخر + فهرست اعضا)، و پیام
  گزارش‌شده با برچسب مشخص علامت خورده است؛
- امکان حذف کامل گروه/کانال را می‌دهد. حذف نرم است: چت از فهرست همه اعضا حذف
  می‌شود و پیام‌ها دیگر سرو نمی‌شوند، اما ردیف‌ها برای ممیزی باقی می‌مانند؛
- چهار وضعیت گزارش را با دکمه تغییر می‌دهد (در انتظار / بررسی‌شده / اقدام‌شده /
  رد شده) و زمان رسیدگی را ثبت می‌کند؛
- یادداشت مدیر را ذخیره می‌کند؛
- با حذف کانال، گزارش‌های باز مربوط به آن خودکار «اقدام‌شده» می‌شوند.

همه اقدام‌ها ممیزی (`AuditLog`) و با CSRF محافظت می‌شوند. چت خصوصی از این مسیر
قابل حذف نیست. **۸ تست جدید.**

## ۳. خروج از راه دور واقعاً انجام نمی‌شد

**علت ریشه‌ای (باگ ظریف):** پس از پایان‌دادن به یک دستگاه، سرور به *هر* درخواست آن
دستگاه پاسخ ۴۰۱ می‌دهد — از جمله درخواست‌هایی که خودِ فرایند خروج می‌زند.
`handleRemoteTermination()` منتظر `PushService.unregisterToken()` می‌ماند؛ ۴۰۱ آن
دوباره همین متد را فراخوانی می‌کرد، `_sessionGeneration` را یک واحد جلو می‌برد، و
در نتیجه `_clearSession(generation)` بیرونی در بررسی `_isCurrent` رد می‌شد و
**هیچ‌چیز پاک نمی‌کرد**. نشست زنده می‌ماند و گوشی در فهرست چت باقی می‌ماند.

بررسی شد که سمت سرور درست کار می‌کند (اسکریپت بازتولید: `terminate` → دستگاه دوم
روی همه مسیرها ۴۰۱ با کد `device_terminated` می‌گیرد)، پس اصلاح فقط سمت کلاینت بود.

**اصلاح:** یک قفل عدم‌ورود مجدد اضافه شد و ترتیب عوض شد — وضعیت **قبل از** هر
تماس شبکه‌ای پاک می‌شود، پس هیچ ۴۰۱ بعدی نمی‌تواند خروج را خنثی کند.
**۵ تست جدید فلاتر**، از جمله سناریوی چند فراخوانی هم‌زمان.

## ۴. به‌روزرسانی خودکار در گروه‌ها و کانال‌ها

**علت ریشه‌ای (اثبات‌شده با اسکریپت بازتولید):** اگر پیامی که مکان‌نمای poll به آن
اشاره می‌کرد سخت‌حذف می‌شد، `GET /messages/<chat_id>?after_id=…` برای همیشه ۴۰۴
برمی‌گرداند. poll هر ۳ ثانیه همان ۴۰۴ را می‌گرفت و هرگز بازیابی نمی‌شد — دقیقاً
همان رفتاری که گزارش کردید: تا از چت بیرون نروید و برنگردید، پیام جدید نمی‌بینید.

**اصلاح:** مکان‌نمای گم‌شده حالا به فیلتر زمانی (پارامتر جدید `after_since`) یا
تازه‌ترین صفحه تنزل می‌کند و `cursor_reset: true` برمی‌گرداند تا کلاینت مکان‌نما را
جایگزین کند. کلاینت هم `_lastMessageAt` را نگه می‌دارد و روی ۴۰۴ بازیابی می‌کند.

دو مرز مهم عمداً حفظ شد: مکان‌نمای متعلق به **چت دیگر** همچنان ۴۰۴ می‌گیرد (تا
بازیابی به راهی برای خواندن بین چت‌ها تبدیل نشود)، و `before_id` هم همچنان ۴۰۴
می‌دهد چون ورق‌زدن تاریخچه کنش کاربر است نه poll. **۶ تست جدید.**

## ۵. متن فارسی

دو مشکل جدا بود:

**الف) اعلان‌ها همیشه فارسی بودند.** اعلان‌ها در ایزوله‌های پس‌زمینه ساخته می‌شوند
که هیچ `BuildContext` و در نتیجه هیچ `Localizations` ندارند، پس متن‌ها ثابت فارسی
کدنویسی شده بود. یک `AppLocale` اضافه شد: آینه‌ای از زبان انتخابی که به
`BuildContext` نیاز ندارد، با هر تغییر زبان به‌روز می‌شود و ایزوله‌های پس‌زمینه از
تنظیمات می‌خوانندش. تمام متن‌های اعلان دوزبانه شدند. نام کانال اعلان اندروید عمداً
خنثی شد، چون اندروید آن را در اولین ساخت کش می‌کند.

**ب) متن فارسی خراب نمایش داده می‌شد.** سه رشته پیدا شد که **کاراکترهایشان معکوس
ذخیره شده بود** و به‌صورت نامفهوم رندر می‌شدند — دقیقاً شامل همان پیام «پس‌زمینه
تنظیم شد» که بعد از تغییر پس‌زمینه گزارش کردید (`'کب‌دنارگ میظنت دش'`)، به‌علاوه
پیام‌های بلاک و آنبلاک کاربر. هر سه اصلاح و به `ChatLabels` دوزبانه منتقل شدند.

## ۶. هیچ‌چیز ذخیره نمی‌شد (بحرانی)

**علت ریشه‌ای:** کد ذخیره از `getDownloadsDirectory()` استفاده می‌کرد که **روی
اندروید همیشه null است**، و در نتیجه فایل‌ها در پوشه خصوصی برنامه می‌افتادند — جایی
که هیچ گالری یا فایل‌منیجری نمی‌بیند. هیچ خطایی هم برگردانده نمی‌شد، پس رابط کاربری
«ذخیره شد» نشان می‌داد. مجوزی هم اصلاً درخواست نمی‌شد.

**اصلاح — زنجیره کامل:**
- کانال `secure_messenger/media_store` در `MainActivity.kt` که فایل را در
  MediaStore منتشر می‌کند (Pictures/Movies/Music/Download زیر پوشه
  `SecureMessenger`)، با مسیر قدیمی برای اندروید ۹ و پایین‌تر و `IS_PENDING` تا
  فایل نیمه‌نوشته دیده نشود؛
- سرویس `GallerySaverService` که مجوز درست هر نسخه اندروید را می‌گیرد (و روی
  اندروید ۱۰+ اصلاً مجوز نمی‌خواهد، چون MediaStore به آن نیاز ندارد و دیالوگ روی
  ۱۳+ اساساً قابل تأیید نیست)؛
- شکست حالا `MediaSaveException` می‌اندازد و `SaveFeedback` پیام واقعی دوزبانه با
  دکمه «باز کردن تنظیمات» نشان می‌دهد — دیگر ادعای دروغین موفقیت وجود ندارد؛
- هر ۶ نقطه ذخیره (عکس، ویدیو، ویس، موسیقی، فایل، عکس پروفایل) وصل شدند و فایل
  بر اساس نوع پیام به مجموعه درست می‌رود.

**۱۱ تست فلاتر جدید**، متمرکز بر همان چیزی که قبلاً غلط بود: رد مجوز یا خطای پلتفرم
هرگز نباید «موفق» گزارش شود.

## ۷. چت «پشتیبانی امنیتی»

کدهای ورود و هشدارهای امنیتی دیگر در «پیام‌های ذخیره‌شده» نوشته نمی‌شوند. یک چت
سرویس اختصاصی از نوع `security` ساخته شد که:
- **یک‌طرفه** است — سرور می‌نویسد، کاربر فقط می‌خواند. سمت سرور اجبار می‌شود
  (`can_send` رد می‌کند) و در رابط کاربری هم نوار نوشتن پنهان است؛
- در اولین نیاز خودکار ساخته می‌شود، پس حساب‌های موجود هم آن را می‌گیرند؛
- با آیکون سپر و عنوان متناسب با زبان برنامه در فهرست چت دیده می‌شود؛
- شمارنده پیام نخوانده‌اش درست کار می‌کند (چون پیام‌ها به نام خود کاربر ذخیره
  می‌شوند، فیلتر معمولی «فرستنده من نیستم» آن را صفر می‌کرد)؛
- کاربر می‌تواند اعلان‌های خودش را پاک کند، مثل تلگرام.

**۶ تست جدید.**

---

## تأیید

- ۲۸۵ تست بک‌اند سبز (`pytest`).
- نکته: SDK فلاتر در این محیط در دسترس نیست، بنابراین تست‌های Dart نوشته و از نظر
  ساختاری بررسی شده‌اند اما اجرا نشده‌اند. اصلاح‌های سمت سرور همگی اجرا و اثبات
  شده‌اند.
- موارد ۳، ۴ و ۶ پیش از اصلاح با اسکریپت بازتولید اثبات شدند، نه فقط از روی
  بازخوانی کد.
