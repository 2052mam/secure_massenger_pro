# گزارش وضعیت — دور دوم (شش مورد)

تاریخ: ۲۰۲۶-۰۹-۱۸ · شاخه: `arena/01a0b453-secure-massenger-pro`

همه‌ی شش مورد پیاده‌سازی، تست و روی گیت‌هاب منتشر شد. مرجع رفتاری در همه‌ی
موارد تلگرام بوده است.

---

## ۱) شمارش معکوس ۵ دقیقه‌ای در صفحه‌ی کد

صفحه‌ی `phone_verification_screen.dart` بازنویسی شد. حالا یک تایمر زنده
`mm:ss` نشان می‌دهد تا چه زمانی می‌توان کد را دوباره درخواست کرد. تا پایان
شمارش، دکمه‌ی «ارسال دوباره» اصلاً نمایش داده نمی‌شود و به‌جای آن متن
«ارسال دوباره کد تا 04:59 دیگر» دیده می‌شود.

- مقدار اولیه از پاسخ سرور (`resend_after_seconds`) خوانده می‌شود؛ پیش‌فرض
  سرور از ۶۰ ثانیه به **۳۰۰ ثانیه (۵ دقیقه)** تغییر کرد تا با خواسته‌ی شما
  یکی باشد (`backend/app/__init__.py`, `.env`, `.env.example`).
- اگر سرور خطای ۴۲۹ بدهد، مقدار `retry_after_seconds` آن روی تایمر می‌نشیند،
  پس شمارش هیچ‌وقت با وضعیت واقعی سرور اختلاف پیدا نمی‌کند.
- تایمر در `dispose` لغو می‌شود (نشتی و خطای «setState after dispose» ندارد).

## ۲) برش تصویر هنگام انتخاب عکس پروفایل

صفحه‌ی جدید `lib/presentation/screens/profile/avatar_crop_screen.dart`:
یک برش‌دهنده‌ی **دایره‌ای به سبک تلگرام** با کشیدن، بزرگ‌نمایی دو انگشتی و
یک اسلایدر زوم.

- محدوده‌ی جابه‌جایی به‌گونه‌ای مهار (clamp) شده که دایره همیشه کاملاً پر
  بماند؛ یعنی کاربر نمی‌تواند فضای خالی را برش بزند.
- خروجی یک JPEG مربعی ۷۲۰×۷۲۰ با کیفیت ۹۰ است.
- هم در «عکس پروفایل اصلی» (`profile_screen.dart`) و هم در «آلبوم عکس‌ها»
  (`profile_photos_screen.dart`) قبل از آپلود اجرا می‌شود.
- سقف رزولوشن انتخاب از ۸۰۰ به ۲۰۴۸ پیکسل رفت، چون برش‌دهنده خودش در انتها
  کوچک می‌کند و دیگر دلیلی برای از دست دادن جزئیات نیست.
- بدون افزودن هیچ پکیج جدیدی ساخته شد (روی `image: ^4.2.0` موجود).

## ۳) هشدار ورود دستگاه دیگر + ارسال کد داخل برنامه

**دلیل دیرکرد هشدار:** بنر قبلی فقط یک‌بار هنگام ساخته‌شدن ویجت
`/devices/notifications` را صدا می‌زد؛ اگر کاربر همان موقع در فهرست گفتگوها
بود، هشدار تا ری‌استارت کامل برنامه دیده نمی‌شد.

- سرویس تازه‌ی `backend/app/services/security_alerts.py` و اندپوینت
  `GET /api/v1/security/alerts` ساخته شد.
- بنر جدید `_SecurityAlertBanner` هر **۲۰ ثانیه** این فید را می‌خواند، پس
  هشدار عملاً بی‌درنگ روی فهرست گفتگوها می‌آید. رنگ بنر با شدت رویداد
  (`critical` قرمز / `warning` نارنجی) تغییر می‌کند، با ضربه به صفحه‌ی
  دستگاه‌ها می‌رود و بستن آن به سرور هم اطلاع داده می‌شود تا روی دستگاه دیگر
  دوباره ظاهر نشود.
- انواع رویداد: `new_device_login`، `login_code_requested`،
  `two_factor_enabled/disabled`، `device_terminated`، `primary_device_changed`،
  `archive_lock_changed`.

**ارسال کد داخل برنامه:** `backend/app/services/code_delivery.py` اضافه شد.
اگر شماره‌ای از قبل حساب دارد و روی دستگاهی با فعالیت ۶۰ روز اخیر نشست زنده
دارد، کد ورود **به‌جای پیامک داخل برنامه** فرستاده می‌شود — دقیقاً مثل
تلگرام. پاسخ سرور `delivery_channel: 'in_app' | 'sms'` برمی‌گرداند و صفحه‌ی
کد متن و آیکون خود را بر همان اساس عوض می‌کند و دکمه‌ی «ارسال کد با پیامک»
را به‌عنوان راه فرار نشان می‌دهد.

## ۴) دستگاه اصلی (Primary device)

- `UserDevice` دو ستون `is_primary` و `primary_since` گرفت و
  `schema_upgrade.py` برای پایگاه‌داده‌های موجود backfill می‌کند؛ یعنی
  قدیمی‌ترین دستگاه هر حساب به‌طور خودکار «اصلی» می‌شود.
- اولین دستگاهی که وارد شود، اصلی است. دستگاه‌های دیگر **نمی‌توانند** آن را
  حذف کنند (`403 cannot_terminate_primary`)؛ فقط خودِ دستگاه اصلی می‌تواند.
- «پایان همه نشست‌های دیگر» هرگز دستگاه اصلی را نمی‌بندد.
- **تأیید دو مرحله‌ای** و **قفل چت‌های بایگانی** فقط از دستگاه اصلی
  (`403 primary_device_required`).
- `POST /api/v1/devices/primary/transfer` برای انتقال آگاهانه‌ی مالکیت.
- صفحه‌ی `device_management_screen.dart` بازنویسی شد: نشان «اصلی»، بنر
  وضعیت، غیرفعال‌شدن دکمه‌های غیرمجاز و دکمه‌ی انتقال. صفحه‌ی تأیید دو
  مرحله‌ای هم پیش از تلاش، وضعیت را می‌پرسد و توضیح می‌دهد.

## ۵) بازسازی کامل پنل مدیریت

پنل از صفر نوشته شد: `backend/app/admin/ui.py` (قالب مشترک) و
`backend/app/admin/routes.py` (~۱۱۰۰ خط).

**رابط کاربری:** طرح تیره‌ی راست‌به‌چپ با سایدبار ثابت و ۱۰ بخش. داشبورد
۱۲ کاشی آمار، نمودار ۱۴ روزه‌ی پیام‌ها و نمودار میله‌ای توزیع چت/نوع پیام
دارد.

**بخش پشتیبانی (خواسته‌ی شما):** دو زبانه —
- «چت‌های پشتیبانی»: گفتگوهایی که کاربران با پشتیبانی دارند، با نمای کامل
  رشته و **فرم پاسخ‌دادن** از داخل پنل.
- «تیکت‌ها»: پیام‌های ارسالی از صفحه‌ی ورود (مورد ۶) با تغییر وضعیت
  (باز / در حال بررسی / حل‌شده / بسته).

**فیلتر و مرتب‌سازی:** روی همه‌ی فهرست‌ها — کاربران (جستجو، وضعیت،
مسدود/محدود)، چت‌ها (نوع، وضعیت)، پیام‌ها (متن، نوع، چت)، گزارش‌ها،
رسانه‌ها، دستگاه‌ها (اصلی/غیر اصلی)، رویدادهای امنیتی (شدت) و لاگ سیستم
(اکشن و نوع موجودیت، که فهرست گزینه‌هایش از مقادیر واقعی دیتابیس ساخته
می‌شود). صفحه‌بندی روی همه هست و فیلترها در لینک صفحه‌ی بعد حفظ می‌شوند.

**امنیت پنل:** مسیر مخفی با مقایسه‌ی `hmac.compare_digest` (عدم تطابق =
۴۰۴ خالی)، توکن CSRF روی همه‌ی POSTها، محدودیت ۵ تلاش ناموفق ورود با قفل
۱۰ دقیقه‌ای، مقایسه‌ی رمز در زمان ثابت، فرار کامل HTML روی محتوای کاربر، و
ثبت هر عملیات در لاگ حسابرسی با شناسه‌ی `panel:<user>`.

**عملیات:** مسدود/رفع مسدودی، محدودیت ۷ روزه، خروج اجباری از همه‌ی نشست‌ها،
اعطای نقش پشتیبانی، تعلیق/اسپانسر کردن چت، حذف پیام برای همه — همگی به‌صورت
فرم POST با تأیید.

## ۶) تماس با پشتیبانی از صفحه‌ی ورود و صفحه‌ی کد

`lib/presentation/screens/auth/support_contact_sheet.dart` — یک شیت که
**بدون نیاز به ورود** کار می‌کند. موضوع‌ها از `GET /api/v1/support/topics`
می‌آید (با فهرست پشتیبان محلی برای وقتی سرور در دسترس نیست) و شامل
«کد تأیید را دریافت نکردم» است.

- در **صفحه‌ی ورود**: «مشکلی در ورود دارید؟ تماس با پشتیبانی».
- در **صفحه‌ی کد**: «کد به دستم نمی‌رسد — تماس با پشتیبانی»، با شماره‌ی
  در حال تأیید که از قبل پر شده است.
- سقف ۵ تیکت در روز برای هر شماره؛ تیکت‌ها مستقیم در زبانه‌ی «تیکت‌ها»ی
  پنل مدیریت دیده می‌شوند.

---

## تست‌ها

| مجموعه | تعداد | وضعیت |
|---|---|---|
| کل تست‌های بک‌اند (pytest) | **۲۶۵** | ✅ همه سبز |
| `test_admin_panel.py` (جدید) | ۲۹ | ✅ |
| `test_support_and_admin.py` (جدید) | ۲۶ | ✅ |
| `test_primary_device.py` (جدید) | ۹ | ✅ |
| `test_code_delivery_and_alerts.py` (جدید) | ۱۲ | ✅ |
| تست‌های ویجت فلاتر (جدید) | ۳ فایل | نوشته شد |

فایل‌های تست فلاتر جدید: `test/auth/code_countdown_and_support_test.dart`،
`test/auth/primary_device_ui_test.dart`،
`test/chat/security_alert_banner_test.dart`.

> **یک نکته‌ی صادقانه:** در این محیط نه `pub.dev` و نه SDK فلاتر در دسترس
> بود، بنابراین `flutter test` و `flutter analyze` را نتوانستم اجرا کنم.
> کد دارت با یک بررسی‌کننده‌ی توازن براکت/رشته روی کل `lib/` و `test/`
> کنترل شد و همه‌ی ارجاع‌ها به API دستی با امضای واقعی متدها تطبیق داده شد،
> اما تست‌های ویجت هنوز اجرا نشده‌اند. لطفاً یک‌بار `flutter test` را روی
> دستگاه خودتان بزنید. به همین دلیل هم هیچ پکیج جدیدی اضافه نشد و
> برش‌دهنده‌ی تصویر با کتابخانه‌های موجود ساخته شد.

## نکات عملیاتی

- `PHONE_CODE_RESEND_SECONDS` حالا ۳۰۰ است. اگر برای تست عدد کمتری
  می‌خواهید، در `backend/.env` تغییرش دهید.
- برای پاسخ‌دادن از پنل، دست‌کم یک کاربر باید نقش پشتیبانی داشته باشد؛ این
  نقش را از صفحه‌ی «کاربران» پنل بدهید.
- فایل‌های `__pycache__` که سهواً در دور اول کامیت شده بودند از مخزن خارج و
  به `.gitignore` اضافه شدند.
